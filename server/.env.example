ANTHROPIC_API_KEY=your_api_key_here
PORT=3001

# Path prefix when the reverse proxy does not strip it (production on wabradshaw.com)
BASE_PATH=/angry-yellow-fruit-api

# Comma-separated browser origins (optional; defaults include wabradshaw.com + localhost:3000)
# CORS_ORIGINS=https://wabradshaw.com,http://localhost:3000
